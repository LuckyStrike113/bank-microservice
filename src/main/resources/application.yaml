spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/bank_db
    username: postgres
    password: password
  jpa:
    properties:
      hibernate:
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true
    open-in-view : false
    hibernate:
      ddl-auto: none
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
server:
  port: 8081
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
logging:
  level:
    org.apache.coyote.http11: DEBUG
    org.springframework.web: DEBUG
    com.bank.service: DEBUG
exchange-rate:
  api:
    url: https://openexchangerates.org/api
    key: a663ddf94b9945398a68ecaa9d359361